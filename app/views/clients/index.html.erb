<%= render :partial => 'layouts/menu' %>

<%= link_to "Client Nou", :new_client %>
<table>
   <tr>
      <th>ID</th>
      <th>Nume client</th>
      <th> </th>
      <th> </th>
   </tr>   
      <% @clients.each do |c| %>
	<tr>
	 <td><%= c.id %></td>
	 <td><%= c.name %></td>
	 <td><%= link_to 'Editare', edit_client_path(c.id) %></td>
	 <td><%= link_to 'Sterge', { action: :destroy, id: c.id }, method: :delete, data: { confirm: 'Esti sigur?' } %></td>
	</tr>
      <% end %>   
</table>