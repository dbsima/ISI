<%= link_to 'New Division', :new_admin_division %>
<table>
    <tr>
    <th>ID</th>
    <th>Name</th>
    <th> </th>
    </tr>
<% @divisions.each do |u| %>
<tr>
    <td><%= u.id %></td>
    <td><%= u.name %></td>
    <td><%= link_to 'Edit', edit_admin_division_path(u.id) %><td/>
    <td><%= link_to 'Delete', { action: :destroy, id: u.id }, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    
</tr>
<% end %>
</table>
